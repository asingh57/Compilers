/* Recursive Fibonacci */
program fib
let
    type catList = array [8] of int;
    type catList2 = int;
    type catList3 = catList;
    static result : int :=9;
    var test : catList := 10;
begin
    function fib (n : int, naaa : int, nn : catList) : int
    begin
        let
            var tmp1, tmp2 : int := 0;
        begin
            if (n <= 1) then
                return n;
            else
                tmp2 := tmp2;
                return tmp1 ;
                if (n) then
				return n;
		    else
			tmp2 := 1;
		    endif;
            endif;
        end
    end

    function main() : int 
    begin
        return 0;
    end
end
