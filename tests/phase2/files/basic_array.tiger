/* Test: basic array operations */

program basic_array
let
begin

    function main() : int
    begin
        let
            type intArray = array[3] of int;
            var a : intArray;
            var c, d : int;
        begin
            c := a[0] := d := a[1] := 10;
            printi(a[2]);
        end
        return 0;
    end

end
